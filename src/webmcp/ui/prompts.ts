// src/webmcp/ui/prompts.ts
// ────────────────────────────────────────────────────────────────────────────
// 💬 **ชุดคำสั่งตัวอย่างสำหรับกรรมการ** (`D1` ของ scope · มติ D-23 + D-27)
//
// 🔴 **ทำไมต้องเป็นภาษาอังกฤษเสมอ แม้จอจะเป็นภาษาไทย:** คนที่จะพิมพ์ประโยคเหล่านี้คือ
//    **กรรมการต่างชาติ** ⇒ ต้องกดคัดลอกแล้ววางใน ChatGPT ได้ทันทีโดยไม่ต้องแปล
//    ⛔ นี่คือข้อยกเว้นที่ตั้งใจของกติกา i18n ของรีโป — และเหตุผลถูกเขียนไว้ตรงนี้เพื่อไม่ให้
//    ใครมา "แก้ให้ถูกกติกา" แล้วทำให้ของพังโดยไม่รู้ตัว
//
// 🧭 **กรอบเรื่องเล่า: "นักเดินทางมาไทย"** (มติ D-27) — กรรมการสวมบทได้ทันทีโดยไม่ต้องรู้จักไทย
//    และทุกประโยคอยู่ในขอบเขตที่ระบบตอบได้จริง ⛔ ไม่มีประโยคที่เรารู้อยู่แล้วว่าจะล้ม
//
// ⚠️ **ทุกประโยคต้องถูกทดสอบจริงก่อนขึ้นเว็บ** (DoD ของ `D1`: *"≥4 prompts · ทุกอันทดสอบแล้วได้ผลจริง"*)
//    ⛔ ห้ามเพิ่มประโยคที่ยังไม่ได้ยิงกับ agent จริง — ประโยคที่ล้มต่อหน้ากรรมการแย่กว่าไม่มีประโยคนั้น

export interface SuggestedPrompt {
  /** ประโยคที่ผู้ใช้กดคัดลอก — EN เสมอ */
  text: string;
  /** tool ที่ประโยคนี้ตั้งใจให้ agent เรียก — ใช้ตอนไล่เทสว่าครอบคลุมครบทุกชนิดหรือยัง */
  expects: string;
}

/**
 * 🔑 **จงใจไล่ให้ครบทั้ง 3 ชนิดของ tool** — อ่าน · ตัดสินใจ · เขียน
 *    เพราะเกณฑ์ "WebMCP Leverage" ให้คะแนนความหลากหลายของการใช้งาน ไม่ใช่จำนวนการเรียก
 */
/**
 * 🔴 **ทุกประโยคต้องชี้มาที่ *หน้านี้* ⛔ ไม่ใช่ถามลอย ๆ** (เขียนใหม่ทั้งชุด 30 ส.ค. 2026)
 *
 * 🔬 **วัดกับ agent จริงแล้ว ⛔ ไม่ใช่ทฤษฎี** — ชุดเดิมเป็นคำถามธรรมชาติที่ไม่อ้างถึงหน้าเว็บ
 *    ผลคือ **agent เลือกความสามารถในตัวมันเองทุกครั้ง** และหน้าเว็บไม่ขยับเลยสักรอบ:
 *    · `Watch PM2.5 in Bangkok and warn me…` ⇒ ไปสร้าง **งานตั้งเวลาของ ChatGPT เอง** (ตรงกับฟีเจอร์ในตัวพอดี)
 *    · `Compare the air in Bangkok and Chiang Mai` ⇒ ตอบจาก **iqair.com** (ตัวเลขคนละชุดกับของเรา)
 *    · `Use this site's tools to…` ในโหมด Chat ⇒ ไป **ค้นเว็บ** ชื่อโดเมนเราแทนการเรียกเครื่องมือ
 *    ⇒ ประโยคที่ล้มต่อหน้ากรรมการแย่กว่าไม่มีประโยคนั้น — และชุดเดิม **ล้มทั้งชุด**
 *
 * ✅ **รูปแบบที่พิสูจน์แล้วว่าเรียกติด** (เจ้าของยิงเอง 30 ส.ค. · โหมด Work + GPT-5.6 Terra):
 *    *"Use the site tools on this page to check air and hazards for Chiang Mai."*
 *    ⇒ agent เรียก `search_locations` ต่อด้วย `get_environment_snapshot` · จอขยับจริง
 *    ⇒ และมันหยิบ **วันหมดอายุของประกาศ** ที่เราติดไว้ไปตัดสินเองว่าใบนั้นหมดอายุแล้ว
 *
 * 🧭 **ทำไมการเขียนว่า "use the tools on this page" ไม่ใช่การโกง:** โจทย์ของการแข่งคือ
 *    *"เว็บที่ agent ใช้งานได้"* ⇒ การบอก agent ว่าให้ใช้เครื่องมือของหน้าที่เปิดอยู่ **คือการใช้งานตามปกติ**
 *    ⛔ สิ่งที่จะเป็นการโกงคือการอ้างว่ามันเลือกเองทั้งที่ไม่ได้เลือก — ซึ่งเราเขียนไว้ตรง ๆ ใน `copy.tip` แล้ว
 *
 * ⚠️ **สถานะการทดสอบรายประโยค (อัปเดตทุกครั้งที่ยิงจริง):**
 *    · #1 ✅ ยืนยันแล้ว 30 ส.ค. 2026 (รูปประโยคเดียวกับที่เจ้าของยิงสำเร็จ)
 *    · #2-#5 🟡 **ยังไม่ได้ยิงกับ agent จริง** — ใช้รูปประโยคเดียวกับ #1 แต่ต้องยืนยันก่อนส่งจริง
 *      ⛔ ห้ามเขียนที่ไหนว่าทดสอบครบแล้วจนกว่าจะยิงจริงทั้ง 5
 */
export const SUGGESTED_PROMPTS: readonly SuggestedPrompt[] = [
  {
    /** ✅ ประโยคที่ยืนยันแล้วว่าเรียกติด — วางไว้เป็นตัวแรกโดยตั้งใจ ให้คนที่ลองแค่ประโยคเดียวเจอตัวที่ชัวร์ */
    text: 'Use the site tools on this page to check air and hazards for Chiang Mai.',
    expects: 'get_environment_snapshot',
  },
  {
    text: "Using this page's tools, is it safe for a morning run in Bangkok right now?",
    expects: 'get_safety_briefing',
  },
  {
    text: 'With the tools on this page, find “เชียงราย” and show any official hazard alerts there today.',
    expects: 'search_locations',
  },
  {
    text: "Use this page's tools to set a watch on Bangkok PM2.5 above 75.",
    expects: 'create_watch',
  },
  {
    text: "Using this page's tools, list my watches and then remove the Bangkok one.",
    expects: 'list_watches',
  },
] as const;
